# LIC — схемы

> **Что здесь:** определения типов запросов/ответов сервиса LIC. Ручки, ссылающиеся на них — `endpoints/LIC.md`.
> **Источник:** swagger сервиса LIC · файл генерируется пайплайном — руками не править.

```
type WatcherStateEnum enum(Stopped, Started)
```
