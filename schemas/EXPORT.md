# EXPORT — схемы

> **Что здесь:** определения типов запросов/ответов сервиса EXPORT. Ручки, ссылающиеся на них — `endpoints/EXPORT.md`.
> **Источник:** `snapshots/EXPORT.json` · файл генерируется пайплайном — руками не править.

```
type FieldResult { code?: str /* Код поля */, description?: str /* Описание поля */ }
```
